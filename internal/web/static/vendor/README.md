three.js r160.1 (MIT), pinned. three.module.min.js + OrbitControls.js +
STLLoader.js from the three package, vendored per agent-docs/architecture.md:
any JS library is one pinned file, never a package manager.
